# ── Database ──────────────────────────────────────────────────
# Contains live scan data — never commit
threat_intelligence.db

# ── Auto-generated JSON files (recreated on every run) ────────
CISA_KEV.json
enriched_cves.json
matched_cves.json
review_matches.json
previous_state.json
assets.json
alerts.json
asset_changes.json
failed_targets.json
threat_intelligence_output.json

# ── Snapshot files (machine-specific) ────────────────────────
current_assets_snapshot.json
previous_assets_snapshot.json

# ── Environment / API keys ────────────────────────────────────
.env
*.env

# ── Python cache ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# ── Virtual environments ─────────────────────────────────────
venv/
.venv/
env/

# ── IDE ───────────────────────────────────────────────────────
.vscode/
.idea/

# ── Logs ──────────────────────────────────────────────────────
*.log

# ── External Data (cloned repos — too large for Git) ──────────
data/vcdb/

# ── Prediction Model (local only — not part of TI pipeline on GitHub) ────
prediction_output.json
company_profile.json
prediction_model/
MODEL_CARD.md
PREDICTION_OUTPUT_GUIDE.md
tests/test_fair_engine.py

# ── Windows ───────────────────────────────────────────────────
Thumbs.db
desktop.ini
